# PulseMesh
PulseMesh is a local-first telemetry mesh for agents and operators. It collects live APIs, system stats, logs, feeds, network probes, and sensor streams; normalizes them into health/anomaly metrics; maintains baselines/history; and emits JSON, CSV, Markdown, and HTML dashboards.
